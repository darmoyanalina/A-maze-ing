[project]
name = "mazegen"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "you@example.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy (==2.2.6)",
    "pydantic (==2.13.4)"
]

[tool.poetry]
packages = [{include = "mazegen", from = "mazegen-0.1.0-py3-none-any"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
